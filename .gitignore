# Build artifacts — produced by `make build` / `go build`.
/bin/

# Run-time output (CSV reports, generated logs, etc.).
/results/

# Local SMTP credentials — see internal/manual/manual.md §8.3.
mail-config.toml
/.idea/
